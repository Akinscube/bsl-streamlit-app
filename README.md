# bsl-streamlit-app
A British Sign Language (BSL) gesture recognition system using MediaPipe keypoint detection and a machine learning classifier. The model recognizes numbers (0–10) and alphabet signs (excluding H, J, and Y) from single and two-hand gestures.
